{
  "name": "maneki-store",
  "version": "2.2.0",
  "description": "Maneki Store POS — Web App (Coolify)",
  "scripts": {
    "build:check": "npx tsc --noEmit"
  },
  "author": "MordredGH",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3"
  }
}
